// Simple seeding script for in-memory data (run with `node scripts/seed.js`)
const fs = require('fs');

console.log('Seeding sample data (in-memory; restart server to reset)');
console.log('Use API endpoints to add permanent data once database is added.');

// No-op for now; data lives in route modules in memory when server starts
